1. Do not try to verify the application by yourself. Instead, ask the user to verify the application by themselves. You can provide instructions on how to do this, but do not attempt to verify it on their behalf. Building the app is a complex process that requires specific knowledge and tools, and it is best left to the user to ensure that it is done correctly.
2. Do not try linting or formatting the code by yourself. I will help with that later.
3. Prevent the use of 'any', type assertions, and non-null assertions in the code. These practices can lead to potential bugs and make the code less maintainable. Instead, encourage the use of proper types and type checking to ensure that the code is robust and reliable. This will help catch errors early and improve the overall quality of the codebase.
4. Always ensure you write the code base on the style of current existing code. This will help maintain consistency and readability throughout the project. There might be edging cases and specific patterns that are already established in the codebase, so it is important to follow those conventions to ensure that the new code integrates seamlessly with the existing code. This will also make it easier for other developers to understand and work with the code in the future. And it will make the codebase more cohesive and easier to maintain over time.
5. Before implementing a new util, check carefully whether they already exist in the codebase. This will help avoid duplication and ensure that the code remains clean and organized. It is important to take the time to search through the existing codebase to see if there are any utilities that can be reused or adapted for the new functionality. This will save time and effort, and it will also help maintain consistency throughout the project. If a suitable utility already exists, it can be modified or extended as needed, rather than creating a new one from scratch. You can also consider using existing popular libraries or frameworks that may already have the functionality you need, rather than reinventing the wheel. This will help ensure that your code is efficient and reliable, and it will also save you time and effort in the long run.
6. When designing the UI, use Shadcn-vue components and motion-v for animation. Take reference from how other components are styled and work on the same pattern. Ensure high quality with 100% responsiveness and accessibility. This will help create a visually appealing and user-friendly interface that is accessible to all users, regardless of their device or abilities. Shadcn-vue provides a wide range of customizable components that can be used to create a consistent and cohesive design, while motion-v can be used to add smooth and engaging animations that enhance the user experience. Ensure smooth and logical flow, we might want to take inspiration from existing design patterns and popular SAAS applications to create an intuitive and efficient user interface.
7. When developing server APIs, never use fetch our own API, instead call to that function, if not exist, extract/wrap the logic code into a function, ensure that if it changes other dependents, they must also be updated.
8. When updating the schemas, please use npm run db:generate to generate the necessary SQL migrations. Never create migration files manually and do not try to modify or alter the generated SQL files, they are auto-generated and might break the migration pipeline if we try to modify them.
9. When implementing a new feature, always consider the potential edge cases and error handling scenarios. This will help ensure that the feature is robust and can handle unexpected inputs or situations gracefully. It is important to think through the different ways users might interact with the feature and how it might fail, and to implement appropriate error handling and validation to prevent issues from arising. This will help improve the overall user experience and make the application more reliable.
10. User requests might not always be very clear, you might want to improvise and ask for clarification if needed, but do not make assumptions about what the user wants. Always confirm with the user before proceeding with any implementation to ensure that you are on the same page and that you are delivering what they actually need. This will help avoid misunderstandings and ensure that the final product meets the user's expectations. If there are any uncertainties or ambiguities in the user's request, it is better to ask for clarification rather than making assumptions that could lead to incorrect implementations. Clear communication is key to successful development and ensuring that the user's needs are met effectively.
11. Always use success({data...}) or throw apiError in the server API, never return {success: true, data...} or {success: false, error...}. This will help maintain consistency in the API responses and make it easier for the client-side code to handle the responses effectively. By using a standardized format for success and error responses, it will be easier to parse and manage the responses on the client side, and it will also help ensure that the API is more robust and reliable.
12. Schemas should always be defined in the schema directory, and we extract common schemas into a separate file if they are used in multiple places. Never create schemas directly in code on both client and server, if needed just use pick on the common schema.